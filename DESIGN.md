# Design System: Codex Harbor

## Overview

**设计基准：原生、克制、状态优先。** Codex Harbor 是连接管理器，不是营销页面。界面应让用户先确认“正在看哪个连接、它是否可用、Codex 是否正在使用”，再查看用量与趋势。使用 macOS 原生控件、系统字体与语义色，保持高信息密度，但用留白、浅色容器和一致的状态标记降低扫描负担。

## Colors

- 背景与文字只用 SwiftUI/AppKit 语义色：`windowBackgroundColor`、`controlBackgroundColor`、`textBackgroundColor`、`primary`、`secondary`、`tertiary`；不要硬编码浅色模式白灰值。
- 蓝色是选择、主操作、连接检查和当前趋势的主强调色；绿色表示当前连接或可用；橙色表示过期、低成功率等需要关注；红色表示失败或不可用；紫色只用于 Token 与订阅标签。
- 容器描边通常为 `primary` 的 7.5%–10%；选中描边蓝色 44%–48%；大面积底色使用对应语义色的 3%–14%。颜色必须传达状态，不作装饰性铺色。

## Typography

- 全局使用系统字体；品牌名、连接标题、KPI 数值和总量使用 rounded design，数据列与日期/时间可使用 monospaced 或 `monospacedDigit()`。
- 连接标题 20 pt semibold；趋势总量 21 pt semibold；KPI 数值 19 pt semibold；品牌名 17 pt bold。
- 区块标题使用 callout semibold；正文与状态使用 caption（约 10.5–12 pt）；辅助说明、时间和图例使用 caption2（约 9.5–10 pt）。
- 只在长地址、模型名或密钥等不可换行内容上使用单行截断与最小缩放，不缩小核心状态或操作标签。

## Layout

- 主界面是固定双栏：左侧连接资产栏 `292–315 pt`、理想宽度 `304 pt`；右侧为详情工作区。侧栏背景与竖分隔线贯穿窗口高度，内容内部保留 `34 pt` 标题栏安全区；右侧工具栏总高 `78 pt`，其中可视操作区为 `44 pt`。
- 详情统一采用 `16 pt` 外边距、`12 pt` 区块间距。连接 Hero 高 `120 pt`；概览、趋势、健康面板均使用同一对齐线。
- 右侧分析区宽度达到 `780 pt` 时，趋势图与健康面板并排，间距 `12 pt`；健康面板宽为可用宽度的 28.5%，限制在 `278–304 pt`。更窄时上下堆叠，趋势图至少高 `240 pt`。
- 以 1440×900 为首屏验收尺寸：Hero、5 项 KPI、趋势标题/图表和健康状态必须同时可见；左侧全局信息固定在底部。不得通过增高 Header、扩大卡片或添加第二层工具栏破坏首屏。
- 基础节奏为 4/5/6/7/8/9/10/12/14/16/20 pt；大区块优先 12 或 16 pt，卡片内边距 12–14 pt，紧凑状态组 5–8 pt。

## Elevation & Depth

界面默认扁平，以语义背景层级和 1 pt 低对比描边分隔内容。仅主按钮、已选趋势项和图表交互允许极轻阴影；不要给所有卡片统一加重阴影。图表 Tooltip 使用 `regularMaterial`，其余内容避免玻璃化。

## Shapes

- 大面板与 Hero：连续圆角 `14 pt`；KPI 与列表项：`10 pt`；按钮：`9 pt`；图标底板：`7–12 pt`。
- 状态标签和日期范围使用 Capsule；圆点统一表达状态或数据系列，不替代文字标签。
- 选中列表项除浅蓝底和描边外，左侧保留 `3×24 pt` 蓝色指示条；这是连接选择的固定识别特征。

## Components

### Buttons and segmented controls

- 主按钮为蓝底白字、最小高 `32 pt`、圆角 `9 pt`；次按钮为浅蓝底/蓝字、最小高 `30 pt`。Hero 右侧操作区宽 `258 pt`，顺序固定为“查询（仅托管）/检查/切换或使用中”。
- 模式切换是三等分的账户/托管/API 选择器，高 `36 pt`；选中项蓝色 11% 底、44% 描边，并在真正生效的模式右上角显示 6 pt 绿色点。浏览选择与实际连接状态不可混为一谈。
- 趋势指标使用圆角矩形分段（Token/请求/耗时，单项 `64×30 pt`）；日期范围使用 Capsule（今日/7日/当月，单项 `46×25 pt`）。只高亮一个选项。

### KPI cards

- 固定 5 项且顺序不变：请求数、请求 Token、平均响应、成功率、工作时长。
- KPI 区间距 `8 pt`，卡片最小高 `112 pt`、水平/垂直内边距 `12/10 pt`、圆角 `10 pt`。结构固定为 24 pt 语义图标底板、标签、主数值、真实环比或明细，以及右下角真实 Mini Sparkline。
- 数值跟随日期范围；请求显示成功次数，Token 显示输入/输出，响应显示 P95，成功率显示成功/总数，工作时长说明为请求耗时累计。
- Mini Sparkline 不显示坐标、网格或 Tooltip；有完整时间桶时保留真实零值，只有一个桶时只显示基准与当前点。环比只在上一周期有可比较数据时出现。

### Trend charts

- 日期范围规则：今日从本地零点开始并以 `HH:mm` 标注；7 日包含今天及前 6 天；当月从本月首日开始。日标签为 `M月d日`，最后一天显示“今天”；完整到期时间使用 `yyyy-MM-dd HH:mm:ss`，最近检查用相对时间，活动日志用短时分。
- Token 使用蓝色渐变面积图，请求使用分组柱状图，耗时使用无面积填充的折线图；网格线低对比，主线约 2 pt。顶部显示当前汇总、真实环比和峰值（耗时写“最高”），底部固定显示总计/均值/最高/最低四项摘要。
- 账户与托管展示单系列；API 展示各档案多系列，Provider 颜色按身份永久固定，不随左侧选择、指标或时间范围重新分配。左侧选中档案的系列加粗、置顶并保持全不透明，其他系列维持自身颜色并降至 `40%–60%` 透明度；图例在筛选器下方同步标记“当前”，悬停只临时聚焦曲线，不改变档案选择或当前连接。最多保留 6 项图例，无数据时显示明确空状态，不伪造走势。
- 趋势序列必须先生成完整时间桶；确认“无记录即 0”的 Codex 请求记录会显示为真实 `0 → 峰值 → 0`，禁止把单个非零桶退化为孤立点或用插值伪造趋势。
- Token 数值按中文习惯压缩为“万/亿”，Tooltip 同时提供请求、Token、平均响应和成功率等真实存在的信息。

### Health panel

- 面板顺序固定：总体健康、模式专属状态行、最近 4 组活动。状态行高至少 `34 pt`，活动行至少 `28 pt`。
- 状态层级为：当前连接/未使用 → 可用性 → 模式专属信息 → 最近检查/当前模型。总体状态只表达真实连接检查结果；低成功率和慢响应保留在 KPI，不生成额外大警告卡。
- 账户显示订阅与剩余天数；托管显示余额与到期；API 显示延迟、Provider 与模型。不要跨模式展示不存在的字段。
- 连续相同活动自动聚合为 `×N`，避免重复失败记录占满健康侧栏。

## Do's and Don'ts

### Do

- **Do** 保持三种模式共用同一 Hero、5 项 KPI、趋势与健康骨架，只替换模式专属内容。
- **Do** 同时用图标/文字/颜色表达状态，并为关键图标提供 accessibility label、help 和选中 trait。
- **Do** 将数值变化、选择切换和图表交互限制在约 0.14–0.46 秒；呼吸提示仅用于正在进行或需注意的状态。
- **Do** 尊重“降低动态效果”：循环脉冲与入场位移停用，其余动画降为约 0.01 秒线性过渡。

### Don't

- **Don't** 改变左侧管理连接、右侧查看当前详情的职责边界，也不要把浏览中的档案误标为 Codex 当前连接。
- **Don't** 增删或重排 5 项 KPI，不要让日期范围与趋势范围脱节。
- **Don't** 用自定义字体、渐变大色块、厚阴影或固定浅色值替代 macOS 语义系统。
- **Don't** 在 1440×900 首屏之外隐藏健康摘要或趋势主信息，也不要写入尚未实现的模型目录、自动优化或预测能力。
